stages:
  - tests

taiko:
  stage: tests
  image: t0mmili/taiko-ci:latest-bun
  variables:
    TAIKO_BROWSER_ARGS: '--no-sandbox,--start-maximized,--disable-dev-shm-usage'
    TAIKO_ENABLE_ACTION_OUTPUT: 'true'
  script:
    - taiko tests/taiko-test-1.js
